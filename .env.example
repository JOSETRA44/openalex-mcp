# Copy to .env and fill in. Never commit the filled-in file.


# Contact email for OpenAlex's polite pool, which grants higher rate limits. Optional but recommended.
# Required: no
OPENALEX_EMAIL=

# Premium OpenAlex API key. Optional; the free tier needs none.
# Required: no  |  Secret: keep it out of version control
OPENALEX_API_KEY=
